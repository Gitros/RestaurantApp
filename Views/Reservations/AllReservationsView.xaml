<UserControl x:Class="RestaurantApp.Views.AllReservationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:shared="clr-namespace:RestaurantApp.Views.Shared"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <shared:ListPageShell
        Title="Rezerwacje"
        CountText="{Binding Reservations.Count, StringFormat='Liczba rezerwacji: {0}'}">

        <DataGrid ItemsSource="{Binding Reservations}"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ReservationId}" Width="60" />
                <DataGridTextColumn Header="Stolik" Binding="{Binding TableId}" Width="80" />
                <DataGridTextColumn Header="Klient" Binding="{Binding CustomerId}" Width="80" />
                <DataGridTextColumn Header="Data" Binding="{Binding ReservationDateTime, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" Width="150" />
                <DataGridTextColumn Header="Osoby" Binding="{Binding GuestsCount}" Width="80" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

    </shared:ListPageShell>
</UserControl>
