<UserControl x:Class="RestaurantApp.Views.AllPaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:shared="clr-namespace:RestaurantApp.Views.Shared"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <shared:ListPageShell
        Title="Płatności"
        CountText="{Binding Payments.Count, StringFormat='Liczba płatności: {0}'}">

        <DataGrid ItemsSource="{Binding Payments}"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding PaymentId}" Width="60" />
                <DataGridTextColumn Header="Zamówienie" Binding="{Binding OrderId}" Width="80" />
                <DataGridTextColumn Header="Metoda" Binding="{Binding PaymentMethodName}" Width="80" />
                <DataGridTextColumn Header="Kwota" Binding="{Binding Amount, StringFormat=\{0:C\}}" Width="100" />
                <DataGridTextColumn Header="Data" Binding="{Binding PaymentDateTime, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" Width="150" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

    </shared:ListPageShell>
</UserControl>
