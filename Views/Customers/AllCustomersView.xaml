<UserControl x:Class="RestaurantApp.Views.AllCustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:shared="clr-namespace:RestaurantApp.Views.Shared"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <shared:ListPageShell
        Title="Klienci"
        CountText="{Binding Customers.Count, StringFormat='Liczba klientów: {0}'}">

        <DataGrid ItemsSource="{Binding Customers}"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"
                                    Binding="{Binding CustomerId}"
                                    Width="60" />

                <DataGridTextColumn Header="Imię"
                                    Binding="{Binding FirstName}"
                                    Width="*" />

                <DataGridTextColumn Header="Nazwisko"
                                    Binding="{Binding LastName}"
                                    Width="*" />

                <DataGridTextColumn Header="Telefon"
                                    Binding="{Binding Phone}"
                                    Width="140" />

                <DataGridTextColumn Header="Email"
                                    Binding="{Binding Email}"
                                    Width="2*" />
            </DataGrid.Columns>
        </DataGrid>

    </shared:ListPageShell>
</UserControl>
