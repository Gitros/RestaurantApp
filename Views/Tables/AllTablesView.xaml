<UserControl x:Class="RestaurantApp.Views.AllTablesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:shared="clr-namespace:RestaurantApp.Views.Shared"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
	
	<shared:ListPageShell
        Title="Stoliki"
        CountText="{Binding Tables.Count, StringFormat='Liczba stolików: {0}'}">

		<DataGrid ItemsSource="{Binding Tables}"
                  IsReadOnly="True">
			<DataGrid.Columns>
				<DataGridTextColumn Header="ID"        Binding="{Binding TableId}" Width="60" />
				<DataGridTextColumn Header="Strefa"    Binding="{Binding AreaId}" Width="80" />
				<DataGridTextColumn Header="Nazwa"     Binding="{Binding Name}" Width="*" />
				<DataGridTextColumn Header="Miejsca"   Binding="{Binding SeatsCount}" Width="80" />
				<DataGridTextColumn Header="Aktywny"   Binding="{Binding IsActive}" Width="80" />
			</DataGrid.Columns>
		</DataGrid>

	</shared:ListPageShell>
</UserControl>
