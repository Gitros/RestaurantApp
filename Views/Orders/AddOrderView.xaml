<UserControl x:Class="RestaurantApp.Views.AddOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shared="clr-namespace:RestaurantApp.Views.Shared">

    <shared:ListPageShell Title="Dodaj zamówienie"
                          CountText="{Binding Items.Count, StringFormat='Pozycji: {0}'}">

        <shared:ListPageShell.ToolbarContent>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding TotalAmount, StringFormat='Suma: {0:C}'}"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"/>

                <Button Grid.Column="1"
                        Content="Zapisz"
                        Width="120"
                        Command="{Binding SaveCommand}" />
            </Grid>
        </shared:ListPageShell.ToolbarContent>

        <Grid Margin="6">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Sekcja danych nagłówka -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*"/>
                    <ColumnDefinition Width="1.2*"/>
                    <ColumnDefinition Width="1.6*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Stolik"/>
                    <ComboBox ItemsSource="{Binding Tables}"
                              SelectedItem="{Binding SelectedTable}"
                              DisplayMemberPath="Name"
                              Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="Kelner"/>
                    <ComboBox ItemsSource="{Binding Waiters}"
                              SelectedItem="{Binding SelectedWaiter}"
                              DisplayMemberPath="FirstName"
                              Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Klient (opcjonalnie)"/>
                    <ComboBox ItemsSource="{Binding Customers}"
                              SelectedItem="{Binding SelectedCustomer}"
                              DisplayMemberPath="LastName"
                              Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>

            <!-- Dodawanie pozycji -->
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>

                <ComboBox ItemsSource="{Binding MenuItems}"
                          SelectedItem="{Binding SelectedMenuItem}"
                          DisplayMemberPath="Name"/>

                <TextBox Grid.Column="1"
                         Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"
                         Margin="10,0,0,0"/>

                <Button Grid.Column="2"
                        Content="Dodaj pozycję"
                        Command="{Binding AddItemCommand}"
                        Margin="10,0,0,0"/>

                <Button Grid.Column="4"
                        Content="Usuń"
                        Command="{Binding RemoveItemCommand}"
                        Margin="0,0,0,0"/>
            </Grid>

            <!-- Lista pozycji -->
            <DataGrid Grid.Row="2"
                      ItemsSource="{Binding Items}"
                      SelectedItem="{Binding SelectedRow}"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Pozycja" Binding="{Binding MenuItemName}" Width="*" />
                    <DataGridTextColumn Header="Ilość" Binding="{Binding Quantity}" Width="80" />
                    <DataGridTextColumn Header="Cena" Binding="{Binding UnitPrice, StringFormat=\{0:C\}}" Width="120" />
                    <DataGridTextColumn Header="Razem" Binding="{Binding TotalPrice, StringFormat=\{0:C\}}" Width="120" />
                </DataGrid.Columns>
            </DataGrid>

        </Grid>

    </shared:ListPageShell>
</UserControl>
